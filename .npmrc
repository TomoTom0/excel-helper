legacy-peer-deps=false
# Enforce strict peer dependency checks to catch potential compatibility issues early
# This helps prevent runtime errors caused by mismatched peer dependencies
strict-peer-deps=true
